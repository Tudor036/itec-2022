import { ReactElement } from "react";

interface CampusCardProps {
    campusId: number,
    campusName: string,
    campusLocation: string
}

function CampusCard({campusId, campusName, campusLocation }: CampusCardProps): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default CampusCard;